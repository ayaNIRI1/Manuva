# دليل الإعداد والتشغيل - Manuva Frontend

## المتطلبات الأساسية 📋

قبل البدء، تأكد من تثبيت:

- **Node.js**: الإصدار 18 أو أحدث
- **npm** أو **yarn**: أداة إدارة الحزم
- **Git**: لاستنساخ المشروع

## خطوات الإعداد السريع 🚀

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd manuva-frontend
```

### 2. تثبيت المكتبات

```bash
npm install
# أو
yarn install
```

### 3. إعداد ملف البيئة

```bash
# نسخ ملف البيئة النموذجي
cp .env.example .env
```

تعديل ملف `.env` حسب إعداداتك:

```env
NEXT_PUBLIC_CURRENCY_SYMBOL = 'دج'
NEXT_PUBLIC_API_URL = 'http://localhost:8000/api'
NEXT_PUBLIC_APP_NAME = 'Manuva'
NEXT_PUBLIC_APP_DESCRIPTION = 'منصة المنتجات الحرفية الجزائرية'
```

### 4. تشغيل المشروع

#### وضع التطوير (Development):
```bash
npm run dev
# أو
yarn dev
```

الموقع سيعمل على: **http://localhost:3000**

#### وضع الإنتاج (Production):
```bash
# بناء المشروع
npm run build

# تشغيل النسخة المبنية
npm start
```

## البنية الأساسية للمشروع 📁

```
manuva-frontend/
├── 📂 app/                      # Next.js App Router
│   ├── 📂 (public)/            # الصفحات العامة
│   │   ├── page.jsx            # الصفحة الرئيسية
│   │   ├── shop/               # صفحات المتجر
│   │   ├── cart/               # صفحة السلة
│   │   ├── product/            # صفحات المنتجات
│   │   └── orders/             # صفحة الطلبات
│   ├── 📂 admin/               # لوحة تحكم الإدارة
│   ├── 📂 store/               # لوحة تحكم الحرفيين
│   ├── layout.jsx              # Layout الرئيسي
│   ├── globals.css             # الأنماط العامة
│   └── StoreProvider.js        # Redux Provider
│
├── 📂 components/               # المكونات المشتركة
│   ├── Navbar.jsx              # شريط التنقل
│   ├── Footer.jsx              # التذييل
│   ├── Hero.jsx                # قسم البطل
│   ├── ProductCard.jsx         # بطاقة المنتج
│   ├── CategoriesMarquee.jsx   # عرض الفئات
│   └── ...                     # مكونات أخرى
│
├── 📂 assets/                   # الأصول (صور، أيقونات)
│   ├── assets.js               # البيانات التجريبية
│   └── *.png, *.jpg            # الصور
│
├── 📂 lib/                      # المكتبات والوظائف المساعدة
│   ├── store.js                # Redux Store
│   └── features/               # Redux Slices
│       ├── cart/               # إدارة السلة
│       ├── product/            # إدارة المنتجات
│       └── ...
│
├── 📄 package.json              # معلومات المشروع والمكتبات
├── 📄 next.config.mjs           # إعدادات Next.js
├── 📄 .env.example              # ملف البيئة النموذجي
├── 📄 README.md                 # التوثيق الأساسي
├── 📄 PROJECT_OVERVIEW.md       # نظرة عامة على المشروع
└── 📄 DESIGN_PREVIEW.html       # معاينة التصميم
```

## التقنيات والمكتبات المستخدمة 🛠️

### Framework & Libraries:
- **Next.js 15.3.5**: React framework
- **React 19**: مكتبة الواجهات
- **Redux Toolkit**: إدارة الحالة
- **Tailwind CSS 4**: التصميم

### المكتبات الإضافية:
- **lucide-react**: الأيقونات
- **recharts**: الرسوم البيانية
- **react-hot-toast**: الإشعارات
- **date-fns**: التعامل مع التواريخ

## الميزات الرئيسية ✨

### 1. نظام الألوان
```javascript
// الألوان الرئيسية
primary: orange-600     // اللون الأساسي
secondary: red-600      // اللون الثانوي
accent: amber-500       // لون التمييز
```

### 2. المكونات الأساسية

#### Navbar (شريط التنقل)
- شعار Manuva مع أيقونة
- قائمة التنقل بالعربية
- شريط البحث
- عداد السلة
- زر تسجيل الدخول

#### Hero Section (قسم البطل)
- تصميم gradient دافئ
- عنوان رئيسي بالعربية
- وصف المنصة
- زر الإجراء الرئيسي
- عرض الأسعار بالدينار

#### Product Cards (بطاقات المنتجات)
- صورة المنتج
- العنوان والفئة
- السعر (قبل وبعد الخصم)
- زر الإضافة للسلة
- تأثيرات hover

#### Footer (التذييل)
- معلومات الشركة
- روابط الأقسام
- معلومات التواصل
- أيقونات وسائل التواصل

### 3. البيانات التجريبية

الموجودة في `assets/assets.js`:

```javascript
// الفئات
categories = ["الفخار", "المنسوجات", "النحاس", ...]

// المنتجات النموذجية
productDummyData = [...]

// المتاجر النموذجية
storesDummyData = [...]
```

## الأوامر المتاحة 📝

```bash
# تشغيل وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# تشغيل النسخة المبنية
npm start

# فحص الأكواد (Linting)
npm run lint
```

## نصائح التطوير 💡

### 1. إضافة صفحة جديدة:
```bash
# مثال: إضافة صفحة "من نحن"
app/(public)/about/page.jsx
```

### 2. إضافة مكون جديد:
```bash
# مثال: إضافة مكون "Newsletter"
components/Newsletter.jsx
```

### 3. استخدام الألوان:
```jsx
// استخدم classes Tailwind مع ألوان Manuva
<button className="bg-gradient-to-r from-orange-600 to-red-600 text-white">
  زر
</button>
```

### 4. إضافة أيقونة:
```jsx
import { Heart, ShoppingCart } from 'lucide-react';

<Heart className="text-orange-600" size={20} />
```

## معاينة التصميم 🎨

لمعاينة التصميم الجديد، افتح الملف:
```
DESIGN_PREVIEW.html
```
في المتصفح مباشرة (بدون الحاجة لتشغيل المشروع).

## الربط مع Backend API 🔗

### إعداد API:
1. تأكد من تشغيل Backend على المنفذ 8000
2. عدل `NEXT_PUBLIC_API_URL` في `.env`
3. استخدم fetch أو axios لطلبات API

### مثال:
```javascript
const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL}/products`);
const products = await response.json();
```

## المشاكل الشائعة وحلولها 🔧

### المشكلة: Port 3000 مستخدم
```bash
# الحل: استخدم منفذ آخر
PORT=3001 npm run dev
```

### المشكلة: خطأ في تثبيت المكتبات
```bash
# الحل: احذف node_modules وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: الصور لا تظهر
```bash
# تأكد من وجود الصور في مجلد assets/
# تأكد من استيراد الصور في assets/assets.js
```

## الخطوات التالية 📈

### للمطورين:
1. ✅ ربط Backend API
2. ✅ إضافة نظام المصادقة
3. ✅ تطوير صفحات المنتجات الكاملة
4. ✅ إضافة نظام الفلترة المتقدم
5. ✅ تطوير لوحة تحكم الحرفيين
6. ✅ إضافة نظام الدفع
7. ✅ تحسين SEO

### للمصممين:
1. ✅ إضافة المزيد من الصور الحقيقية
2. ✅ تحسين الأنيميشن
3. ✅ إضافة micro-interactions
4. ✅ تحسين الـ responsive design

## الدعم والمساعدة 💬

إذا واجهت أي مشاكل:

1. **التوثيق**: اقرأ README.md و PROJECT_OVERVIEW.md
2. **Issues**: افتح issue في GitHub
3. **Email**: contact@manuva.dz
4. **Phone**: +213 555 123 456

---

**صنع بحب للحرفيين الجزائريين 🇩🇿**

Manuva © 2026
