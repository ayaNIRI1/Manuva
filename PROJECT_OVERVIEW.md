# مشروع Manuva - مربوط وجاهز! ✅

## 📦 محتويات المجلد

```
manuva-project-connected/
│
├── manuva-backend/              # Backend API (Express.js + PostgreSQL)
│   ├── .env                     # ✅ إعدادات قاعدة البيانات والخادم
│   ├── src/                     # كود المصدر
│   └── package.json
│
├── manuva-frontend/             # Frontend (Next.js + React + Redux)
│   ├── .env.local              # ✅ تم إنشاؤه - متصل بالـ Backend
│   ├── app/                    # صفحات التطبيق
│   ├── components/             # المكونات
│   └── package.json
│
├── 📖 MANUVA_CONNECTION_GUIDE.md    # دليل شامل مفصل
├── 📖 README_QUICK_START.md         # دليل البدء السريع
│
├── 🚀 start-manuva.bat             # سكريبت تشغيل Windows
├── 🚀 start-manuva.sh              # سكريبت تشغيل Mac/Linux
│
└── 🧪 api-tester.html              # أداة اختبار API
```

## ✅ ما تم إنجازه

1. ✅ **ربط Frontend بالـ Backend**
   - تم إنشاء `.env.local` في manuva-frontend
   - تم ضبط `NEXT_PUBLIC_API_URL` على `http://localhost:3000/api`

2. ✅ **إعداد Backend**
   - الخادم يعمل على port 3000
   - CORS مفعّل للسماح بطلبات من Frontend
   - قاعدة بيانات PostgreSQL جاهزة

3. ✅ **سكريبتات التشغيل**
   - سكريبت Windows (bat) لتشغيل كلا الخادمين
   - سكريبت Mac/Linux (sh) لتشغيل كلا الخادمين

4. ✅ **أدوات الاختبار**
   - ملف HTML لاختبار جميع الـ API endpoints
   - دليل شامل لحل المشاكل

## 🚀 البدء السريع

### خطوة 1: تثبيت المكتبات

```bash
# Backend
cd manuva-backend
npm install

# Frontend
cd manuva-frontend
npm install
```

### خطوة 2: إعداد قاعدة البيانات

```bash
# إنشاء قاعدة البيانات
psql -U postgres -c 'CREATE DATABASE "manuva_v";'

# تهيئة الجداول
cd manuva-backend
npm run init-db
```

### خطوة 3: التشغيل

**الطريقة السهلة:**

- Windows: انقر مرتين على `start-manuva.bat`
- Mac/Linux: `./start-manuva.sh`

**أو يدوياً في نافذتين منفصلتين:**

النافذة 1 (Backend):

```bash
cd manuva-backend
npm run dev
```

النافذة 2 (Frontend):

```bash
cd manuva-frontend
npm run dev
```

### خطوة 4: افتح المتصفح

- **التطبيق**: http://localhost:3001
- **API**: http://localhost:3000/health
- **اختبار API**: افتح ملف `api-tester.html` في المتصفح

## 🔗 الاتصال

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  Frontend (Next.js)                Backend (Express)    │
│  http://localhost:3001   ←→   http://localhost:3000    │
│                                                         │
│  - React UI                      - REST API             │
│  - Redux State                   - PostgreSQL DB        │
│  - Tailwind CSS                  - JWT Auth             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 📚 المستندات

1. **MANUVA_CONNECTION_GUIDE.md** - دليل شامل يحتوي على:
   - شرح مفصل للهيكل
   - جميع الإعدادات المطلوبة
   - قائمة بجميع API endpoints
   - حلول للمشاكل الشائعة
   - نصائح الأمان للإنتاج

2. **README_QUICK_START.md** - دليل سريع للبدء فوراً

## 🔧 الإعدادات الحالية

### Backend (.env)

```
PORT=3000
DB_NAME=manuva_v
DB_USER=postgres
DB_PASSWORD=aya
```

### Frontend (.env.local) ✅

```
NEXT_PUBLIC_API_URL='http://localhost:3000/api'
NEXT_PUBLIC_CURRENCY_SYMBOL='دج'
NEXT_PUBLIC_APP_NAME='Manuva'
```

## 🧪 اختبار الاتصال

1. افتح `api-tester.html` في المتصفح
2. اختبر جميع الـ endpoints
3. تأكد من ظهور ✅ نجح

## ⚠️ ملاحظات مهمة

1. **قاعدة البيانات**: يجب أن يكون PostgreSQL قيد التشغيل
2. **المنافذ**: تأكد من أن المنافذ 3000 و 3001 غير مشغولة
3. **Node Modules**: قم بتشغيل `npm install` في كلا المجلدين
4. **كلمة المرور**: غير `DB_PASSWORD` في `.env` إلى كلمة المرور الخاصة بك

## 🆘 المساعدة

إذا واجهت مشاكل:

1. راجع `MANUVA_CONNECTION_GUIDE.md` - قسم "حل المشاكل"
2. تحقق من سجلات الأخطاء في Terminal
3. جرب `api-tester.html` لاختبار الاتصال

## 🎉 الخلاصة

المشروع مربوط بالكامل وجاهز للعمل! فقط:

1. ثبت المكتبات (`npm install`)
2. أنشئ قاعدة البيانات
3. شغل السكريبت
4. ابدأ العمل! 🚀

---

**تاريخ الربط:** 7 فبراير 2026
**الحالة:** ✅ مربوط ومجهز
